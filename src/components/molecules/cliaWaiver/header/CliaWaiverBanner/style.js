import styled from "styled-components"

import { colors } from "@everlywell/leaves"

const Content = styled.div`
  background-color: ${colors.green1};
  font-size: 14px;
  font-weight: 500;
  line-height: 1.57;
  text-align: right;
  color: ${colors.gray4};
  padding: 4px 74px 4px;
`

export { Content }
