import styled from "styled-components"

import { colors } from "@everlywell/leaves"

const ZeroStateHint = styled.div`
  font-size: 18px;
  font-weight: 300;
  line-height: 1.75;
  text-align: center;
  color: ${colors.gray4};
  margin: 46px 144px;
`

export { ZeroStateHint }
