import styled from "styled-components"
import { TertiaryButton } from "../shared"

const GroupsTopWrapper = styled.div`
  margin: 23px 0 24px;
  display: flex;
  justify-content: space-between;
`

const AddGroupButton = styled(TertiaryButton)`
  padding: 4px 10px;
`

export { GroupsTopWrapper, AddGroupButton }
